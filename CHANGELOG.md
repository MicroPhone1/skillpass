# SkillPass — บันทึกการพัฒนา

## 14 สิงหาคม 2026 — เอาเกณฑ์ DSD จริงเข้าแอป

### ทำแล้ว

- เพิ่ม `docs/research.md` รวมงานวิจัยของทั้งสองทีม แยกชัดว่าตัวเลขไหนยืนยันแล้ว
  ตัวไหนต้องตรวจซ้ำ และตัวไหนยังไม่มีใครเก็บ พร้อมลิงก์แหล่งอ้างอิงทุกตัวเลข
- เพิ่ม `js/data/standards.js` เก็บเกณฑ์มาตรฐานฝีมือแรงงานแห่งชาติ 3 สาขา
  (ช่างไฟฟ้าภายในอาคาร, ช่างไฟฟ้าอุตสาหกรรม, ช่างเชื่อม SMAW) — หมวดภาคความรู้
  ครบตามคู่มือ DSD, เกณฑ์ผ่าน, อัตราค่าจ้างรายระดับ และอายุหนังสือรับรอง
- หน้ารายละเอียดเส้นทางแสดงการ์ด "เกณฑ์มาตรฐานจริงที่ใช้สอบ" เฉพาะเส้นทางที่มี
  ใบรับรองจริงรองรับ พร้อมส่วนต่างค่าจ้างเทียบค่าแรงขั้นต่ำแบบคิดให้เสร็จ
- **แก้ตัวเลขค่าจ้างที่คลาดเคลื่อน** ตามประกาศคณะกรรมการค่าจ้าง ฉบับที่ 13:
  ช่างไฟฟ้าภายในอาคาร 470/595/695 (เดิมเข้าใจว่า 400/500/600) และ
  ช่างเชื่อม SMAW 500/610/685 (เดิมเข้าใจว่า 465/550/630)
- เพิ่ม smoke test กันตัวเลขเก่าหลุดกลับมา และกันการ์ดเกณฑ์ไปโผล่ผิดเส้นทาง
- **แก้ข้อความไทยบนเกียรติบัตรที่ตำแหน่งเคลื่อน** — ชื่อเอกสารถูกวาดด้วยเซริฟละติน
  (Georgia) ซึ่งไม่มีสระ/วรรณยุกต์ไทย บวกกับ `letter-spacing` ที่แทรกช่องว่างหลัง
  ทุกอักขระรวมถึงสระลอย รูปสระจึงหลุดจากพยัญชนะ เปลี่ยนชื่อเอกสารเป็น
  CERTIFICATE / OF ACHIEVEMENT ทั้งสองฉบับ และเพิ่มตัวช่วย `ls()` ที่บังคับ
  letter-spacing เป็นศูนย์เมื่อข้อความเป็นภาษาไทย ใช้กับทุกบรรทัดที่เคยตั้งค่าไว้
- เพิ่ม smoke test ไล่ทุก `<text>` บนใบ ถ้าเจอข้อความไทยที่ยังถูกจัดระยะตัวอักษร
  หรือถูกวาดด้วยเซริฟละติน ให้ทดสอบตก

## 13 สิงหาคม 2026 — แก้อาการ "โหลดไฟล์สคริปต์ไม่สำเร็จ" เมื่อเปิดจากเครื่องอื่น

### ทำแล้ว

- ย้าย TLS handshake จากเธรด accept ไปไว้ในเธรดของแต่ละคำขอ (`ThreadingTLSServer`)
  เดิม `wrap_socket()` ครอบซ็อกเก็ตที่ฟังอยู่ ทำให้ handshake ทุกรายต้องต่อคิว
  บนเธรดเดียว และถ้ามีใครต่อเข้ามาแล้วเงียบไปแม้แค่รายเดียว คนอื่นจะเข้าไม่ได้ทั้งหมด
  (ยืนยันด้วยการทดลอง: โครงเดิมมีคนค้าง 1 ราย → หน้าเว็บเปิดไม่ขึ้นเลย)
- เปิด HTTP/1.1 keep-alive ให้ `Handler` — หน้าเว็บโหลด ES module 36 ไฟล์
  เดิมที่เป็น HTTP/1.0 ต้องเปิด TCP+TLS ใหม่ทุกไฟล์ ซึ่งพังง่ายมากเมื่อผ่าน tunnel
- ตั้ง `daemon_threads` และ timeout ของ handshake/keep-alive กันเธรดค้าง

## 4 สิงหาคม 2026 — ต่อ AI backbone เข้าหน้าผู้เรียน

### ทำแล้ว

- ต่อบทบาท `tutor` เข้าหน้าแชทจริง พร้อมส่งประวัติของห้องนั้นให้โมเดล, แสดงสถานะกำลังตอบ และเก็บคำตอบกลับห้องเดิมแม้ผู้ใช้สลับเส้นทางระหว่างรอ
- ต่อบทบาท `explainer` เข้าหน้าเฉลยข้อสอบ แสดงสถานะกำลังวิเคราะห์ และกันผล async ของข้อเก่าไม่ให้ทับข้อใหม่
- เพิ่มหน้า **ประเมินความสามารถ + แผนพัฒนา** (`#/assessment`) ซึ่งเรียก `assessor` ก่อน แล้วส่งผลวินิจฉัยต่อให้ `coach` จัดแผนตามเวลาที่ผู้เรียนเลือก
- เพิ่มป้ายสถานะ AI บน app shell: `กำลังตรวจ AI`, `AI พร้อม` หรือ `โหมดในเครื่อง` กดเพื่อตรวจใหม่ได้
- เพิ่มป้ายแหล่งที่มาบนคำตอบ/เฉลย/ผลประเมินทุกจุด เพื่อแยกผลจาก AI กับเอนจินในเครื่องอย่างโปร่งใส
- คง fallback เดิมทุกฟีเจอร์: gateway, Ollama, คีย์ หรืออินเทอร์เน็ตมีปัญหา แอปยังใช้งานต่อได้
- เพิ่ม smoke test ตรวจ route ใหม่และยืนยันว่าหน้าจอทั้ง 3 จุดเชื่อมกับ role backbone แล้ว

### ยังไม่ได้ทำ

- ฝั่งอาจารย์: Class, ตารางเรียน และรายชื่อนักเรียน
- การเก็บผลประเมิน/แผนพัฒนาแบบถาวรข้ามอุปกรณ์ (ตอนนี้ cache เฉพาะ session และสร้างใหม่เมื่อหลักฐานเปลี่ยน)
- การแจ้งเตือนเมื่อแผนถึงกำหนด และการติดตามว่าแต่ละขั้นทำสำเร็จแล้วหรือยัง
